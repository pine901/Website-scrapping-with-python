1.Install MongoDB.

2.Confirm that python is installed.

3.After install python try these commands.

    pip install opencv-python
    pip install numpy
    pip install urllib3
    pip install scikit-image
    pip install pymongo
    pip install selenium webdriver_manager    

